package testhaschildren

templ list() {
	if ok := templ.HasChildren(ctx); ok {
		<ul>
			{ children... }
		</ul>
	} else {
		<p>{ "No children" }</p>
	}
}

templ render(names []string) {
	<div>
		@list() {
			<p>{ "Has Children" } </p>
			//		for _, n := range names {
			//			<p>{ n }</p>
			//	}
		}
	</div>
	<div>
		@list()
	</div>
}
